server:
  port: 9100
spring:
  application:
    name: user-auth
  redis:
    host: 192.168.235.129
    port: 6379
    password:
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.235.129:3306/dongyimaidb?useSSL=false&useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true
    username: root
    password: 123456
  main:
    allow-bean-definition-overriding: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
auth:
  ttl: 3600  #token存储到redis的过期时间
  clientId: dongyimai
  clientSecret: dongyimai
  cookieDomain: localhost
  cookieMaxAge: -1
encrypt:
  key-store:
    location: classpath:/dongyimai.jks
    secret: dongyimai
    alias: dongyimai
    password: dongyimai
thymeleaf:
  cache: false
  prefix: classpath:/templates/
  suffix: .html